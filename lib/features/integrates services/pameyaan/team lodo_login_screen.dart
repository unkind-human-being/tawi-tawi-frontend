import 'package:flutter/material.dart';

class TeamLodoLoginScreen extends StatelessWidget {
  const TeamLodoLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Team Lodo Login')),
      body: const Center(
        child: Text('Team Lodo Login - Coming Soon'),
      ),
    );
  }
}
