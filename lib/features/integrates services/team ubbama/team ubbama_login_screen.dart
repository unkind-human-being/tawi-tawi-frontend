import 'package:flutter/material.dart';

class TeamUbbamaLoginScreen extends StatelessWidget {
  const TeamUbbamaLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Team Ubbama Login')),
      body: const Center(
        child: Text('Team Ubbama Login - Coming Soon'),
      ),
    );
  }
}
